name: CI

on:
  push:

permissions:
  contents: read
  packages: write

jobs:
  container:
    runs-on: [self-hosted, Linux, X64, ipsos-devops-01, opensuse, podman]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build container
        run: podman build -f Containerfile -t localhost/astra-ci:${GITHUB_SHA} .

      - name: Smoke test
        run: |
          podman rm -f astra-ci >/dev/null 2>&1 || true
          podman run -d --name astra-ci -p 127.0.0.1:18080:80 localhost/astra-ci:${GITHUB_SHA}
          sleep 2
          curl --fail --silent http://127.0.0.1:18080/astra/ >/dev/null
          curl --fail --silent http://127.0.0.1:18080/astra/p1/website/homepage/ >/dev/null
          podman rm -f astra-ci
          echo "Container smoke test passed"

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Publish SHA image
        run: |
          podman tag localhost/astra-ci:${GITHUB_SHA} ghcr.io/iis-design/astra:${GITHUB_SHA}
          podman push ghcr.io/iis-design/astra:${GITHUB_SHA}
          echo "Published ghcr.io/iis-design/astra:${GITHUB_SHA}"

      - name: Publish main tag and deploy
        if: github.ref == 'refs/heads/main'
        run: |
          podman tag localhost/astra-ci:${GITHUB_SHA} ghcr.io/iis-design/astra:main
          podman push ghcr.io/iis-design/astra:main

          ssh -i /home/gha-runner/.ssh/id_ed25519_deploy -o BatchMode=yes nico@192.168.1.83 '
            cd /srv/apps/astra &&
            docker compose pull &&
            docker compose up -d &&
            docker exec design-proxy wget -qO- http://astra/astra/ >/dev/null &&
            docker exec design-proxy wget -qO- http://astra/astra/p1/website/homepage/ >/dev/null &&
            echo "Astra production deploy OK"
          '
